# Optional. The default implementation is fully local and deterministic.
# Set this only if you extend the LLMClient to call an approved provider.
OPENAI_API_KEY=

# Keep at 0 for deterministic task outputs.
LLM_TEMPERATURE=0
